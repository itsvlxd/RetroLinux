# TODO: Make a networking module for either setting up wifi or ethernet
# and also add support for vlans and custom ip setup like manual setup
# and from here to also reset the cache of dns iwd and networkmanager
# lets try to make this work with networkmanager which works through iwd
